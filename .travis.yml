language: node_js
node_js:
  - "node"
warnings_are_errors: false
cache:
  directories:
    - node_modules
install:
  - npm install
script:
  - npm run build
after_success:
  - npm install -g firebase-tools
  - firebase deploy --token "$FIREBASE_CI_TOKEN"
on:
  branch: master